interface:
  display_name: "Pre-Execution Confirmation YCT"
  short_description: "Require explicit user approval before agent actions"
  default_prompt: "Use $pre-execution-confirmation-yct only for a new root-agent user request that asks for an action. Do not re-run it during context compression, summary recovery, model switching, or continuation of the same request."
